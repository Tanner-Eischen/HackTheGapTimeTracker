# Set at runtime; do not commit real values

# API configuration
# When deploying the frontend separately from API (e.g., Netlify + Render),
# set the API base. Leave empty to use same origin proxy.
VITE_API_BASE_URL=